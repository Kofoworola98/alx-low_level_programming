#include <stdio.h>
#include <time.h>

/**
 * main - This prints numbers from 0 to 9.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;

	for (n = 0; n <=9; n++)
	{
		printf("%d\n", n);
	}

	return 0;
}
